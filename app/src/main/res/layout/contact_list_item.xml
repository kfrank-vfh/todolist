<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/contactPhoto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/person_icon"
        android:minWidth="@dimen/contact_list_item_icon_width"
        android:maxWidth="@dimen/contact_list_item_icon_width"
        android:minHeight="@dimen/contact_list_item_icon_height"
        android:maxHeight="@dimen/contact_list_item_icon_height"/>

    <TextView
        android:id="@+id/contactName"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Contact Name"
        android:textSize="@dimen/contact_list_item_text_size"
        android:textColor="@color/black"
        android:padding="@dimen/contact_list_item_padding"/>

</LinearLayout>